# Plan: `<topic>`

> Status: Draft  
> Scope: `<project / initiative / stage>`  
> Role: focused discussion/design plan

## Goal

State what this plan is trying to clarify, design or prepare.

## Scope

In scope:

- `<scope item>`

## Non-goals

Out of scope:

- `<non-goal>`

## Canonical references

Read first:

```text
<path/to/accepted-doc.md>
```

## Selected source anchors

Use only anchors relevant to this plan:

| Topic | Source anchor | Why relevant |
| --- | --- | --- |
| `<topic>` | `<anchor>` | `<reason>` |

## Decisions already accepted

Do not reopen unless new evidence contradicts them:

```text
<decision>
```

## Discussion clusters

### Cluster 1 — `<name>`

Goal:

```text
<goal>
```

Expected output:

```text
<doc / term / contract / task>
```

### Cluster 2 — `<name>`

Goal:

```text
<goal>
```

Expected output:

```text
<doc / term / contract / task>
```

## Expected outputs

- `<output artifact>`

## Open questions / risks

- `<question or risk>`

## Maturity

Current maturity: `<early draft / ready for discussion / ready for task decomposition>`.
