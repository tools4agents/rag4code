# Source Anchor Map: `<source-name>`

> Status: Draft  
> Source: `<source path / title / URL>`  
> Scope: `<project / initiative / topic>`  
> Role: source navigation and traceability map

## Purpose

Explain why this source was processed and what kind of context it contains.

## How to use

Use this map to find relevant source anchors before reading the full source.

Do not treat raw source discussion as accepted project Source of Truth.

## Source anchor map

| Topic | Source anchors | Notes |
| --- | --- | --- |
| `<topic>` | `<line range / section / page>` | `<short note>` |

## Idea inventory summary

| Idea cluster | Status | Materialized in / Next use |
| --- | --- | --- |
| `<idea cluster>` | `raw / candidate / materialized / parked` | `<doc / plan / task>` |

## Already materialized

List materialized docs, terms, contracts or tasks produced from this source.

```text
<path/to/materialized-doc.md>
```

## Canonical warnings

- Do not treat `<raw term / old framing>` as canonical.
- Use `<accepted term / materialized doc>` instead.

## Open source clusters

Keep only compact pointers to not-yet-materialized source areas.

```text
<cluster> -> <anchors> -> <possible future plan>
```

## Related plans

```text
<path/to/discussion-plan.md>
```
