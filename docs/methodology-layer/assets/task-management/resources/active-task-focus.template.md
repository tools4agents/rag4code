# Active Task Focus

> Status: Active  
> Scope: `<project-or-initiative-scope>`  
> Role: current-focus router, not a task index  
> Updated: YYYY-MM-DD

## Current active focus

Коротко: что делается прямо сейчас.

```text
<1-5 строк: текущий active focus / active stage / active route>
```

## Why this focus matters

Коротко: зачем это делается в текущем SDLC / delivery context.

- `<reason 1>`
- `<reason 2>`
- `<reason 3>`

## Read first

Минимальный route чтения для нового захода в контекст:

1. `<project entry point or task-map>`
2. `<current task file>`
3. `<contracts / architecture / docs entry point if needed>`
4. `<supporting artifact if needed>`

## Execute now

Current execution step:

```text
<конкретное действие или bounded objective>
```

Current task file:

```text
<path/to/current-task.md>
```

If relevant, current SoT / contract / implementation entry point:

```text
<path/to/current-sot-or-entry-point>
```

## Keep in context

Important related task files:

```text
<path/to/related-task-1.md>
<path/to/related-task-2.md>
```

Important supporting docs / contracts / evidence:

```text
<path/to/supporting-doc-or-contract>
<path/to/evidence-output>
```

Follow-up tasks to keep in mind, but not execute now:

```text
<TASK.ID> <short name>
<TASK.ID> <short name>
```

## Current decisions and invariants

```text
<decision or invariant 1>
<decision or invariant 2>
<decision or invariant 3>
```

## Do not do

- `<non-goal / forbidden action 1>`
- `<non-goal / forbidden action 2>`
- `<non-goal / forbidden action 3>`

## Update policy

Update this file when:

- active task changes;
- current execution route changes;
- important read-first context changes;
- major blocker or decision changes what agents should do next.

Do not use this file as:

- full task inventory;
- replacement for `task-map.md`;
- replacement for bounded task files;
- architecture Source of Truth.

## Background

Short stable background only, if needed.

Task index:

```text
<path/to/task-map.md>
```
